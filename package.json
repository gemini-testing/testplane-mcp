{
  "type": "module",
  "private": true,
  "version": "0.0.0",
  "description": "Testplane AI monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run check-formatting && npm run test --workspaces",
    "lint": "npm run check-formatting && npm run lint --workspaces",
    "reformat": "prettier --write .",
    "check-formatting": "prettier --check .",
    "ci:preload-browser": "node scripts/preload-browser.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "@types/node": "^20.8.0",
    "eslint": "^9.26.0",
    "globals": "^16.1.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.32.0",
    "vitest": "^3.1.4"
  }
}
